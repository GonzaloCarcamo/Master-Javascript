'use strict'

function holaMundo(texto) {
    var hola_mundo = "Texto dentro de una función"

    console.log(texto);
    console.log(numero.toString());
    console.log(hola_mundo);
}

var texto = "Hola Mundo, soy una variable global";
var numero = 20;

holaMundo(texto);

// Si genero una variable fuera de una función, puedo llamarla fuera y dentro de ella
// Si genero una variable dentro de una función, sólo puedo llamarla desde la función


